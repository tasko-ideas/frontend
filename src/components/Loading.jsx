import { Content } from "antd/es/layout/layout";
import React from "react";

const Loading = () => {
  return (
    <Content
      style={{
        padding: "auto 0",
        textAlign: "center",
        color: "white",
        fontSize: "2rem",
      }}
    >
      Loading...
    </Content>
  );
};

export default Loading;
